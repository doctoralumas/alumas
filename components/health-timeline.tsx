'use client';import {useEffect,useState} from 'react';
export default function HealthTimeline(){const [items,setItems]=useState<any[]>([]),[from,setFrom]=useState(''),[to,setTo]=useState('');const load=()=>{const q=new URLSearchParams();if(from)q.set('from',from);if(to)q.set('to',to);fetch('/api/health/timeline?'+q).then(r=>r.ok?r.json():[]).then(setItems)};useEffect(()=>{load()},[]);return <div className="page"><div className="page-title"><span className="kicker">Geçmiş</span><h1>Sağlık Geçmişim</h1><p>Randevu, laboratuvar, görüntüleme, tetkik isteği ve bakım olaylarını tek zaman çizelgesinde gör.</p></div><div className="panel date-filter-v16"><label>Başlangıç<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Bitiş<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><button className="primary" onClick={load}>Filtrele</button></div><div className="history-timeline-v19">{items.map((x,i)=><article key={i}><time>{new Date(x.at).toLocaleDateString('tr-TR',{day:'numeric',month:'short',year:'numeric'})}</time><div className="history-dot-v19"/><div className="clinical-card-v19"><span className="kicker">{x.type}</span><h3>{x.title}</h3><p>{x.detail}</p></div></article>)}{!items.length&&<div className="empty">Bu aralıkta kayıt bulunamadı.</div>}</div></div>}
