"use client";
import { useEffect,useState } from "react";
type Slot={id:string;startsAt:string;endsAt:string;type:string};
export default function DoctorAvailabilityManager(){const [items,setItems]=useState<Slot[]>([]);const [msg,setMsg]=useState("");async function load(){const r=await fetch('/api/doctor/availability');if(r.ok)setItems(await r.json())}useEffect(()=>{load()},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const date=String(f.get('date'));const start=String(f.get('start'));const end=String(f.get('end'));const r=await fetch('/api/doctor/availability',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({startsAt:new Date(`${date}T${start}:00`).toISOString(),endsAt:new Date(`${date}T${end}:00`).toISOString(),type:f.get('type')})});const j=await r.json();setMsg(r.ok?'Müsaitlik eklendi.':j.error||'Eklenemedi');if(r.ok){(e.currentTarget as HTMLFormElement).reset();load()}}
return <><form className="availability-form" onSubmit={submit}><input type="date" name="date" required/><input type="time" name="start" required/><input type="time" name="end" required/><select name="type"><option value="both">Online + Klinik</option><option value="online">Online</option><option value="clinic">Klinik</option></select><button className="primary">Saat aç</button></form>{msg&&<div className="inline-message">{msg}</div>}<div className="slot-list">{items.map(s=><div className="slot-row" key={s.id}><div><b>{new Date(s.startsAt).toLocaleDateString('tr-TR',{day:'numeric',month:'long',weekday:'short'})}</b><span>{new Date(s.startsAt).toLocaleTimeString('tr-TR',{hour:'2-digit',minute:'2-digit'})}–{new Date(s.endsAt).toLocaleTimeString('tr-TR',{hour:'2-digit',minute:'2-digit'})}</span></div><span className="status">{s.type==='both'?'Online + Klinik':s.type==='online'?'Online':'Klinik'}</span></div>)}</div></>}
