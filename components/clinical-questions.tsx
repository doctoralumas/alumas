"use client";import {useEffect,useState} from "react";
export default function ClinicalQuestions({patientId,doctorMode=false}:{patientId?:string,doctorMode?:boolean}){const [rows,setRows]=useState<any[]>([]);const [doctors,setDoctors]=useState<any[]>([]);const [msg,setMsg]=useState('');const qs=doctorMode&&patientId?`?patientId=${patientId}`:'';const load=()=>fetch('/api/health/questions'+qs).then(r=>r.ok?r.json():[]).then(setRows);useEffect(()=>{load();if(!doctorMode)fetch('/api/health-sharing/doctors').then(r=>r.ok?r.json():[]).then(setDoctors)},[patientId,doctorMode]);async function ask(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch('/api/health/questions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await r.json();setMsg(r.ok?'Sorun uzmana iletildi.':j.error||'GÃ¶nderilemedi');if(r.ok){form.reset();load()}}async function answer(id:string,e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch('/api/health/questions',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,answer:f.get('answer')})});const j=await r.json();setMsg(r.ok?'YanÄ±t kaydedildi.':j.error||'YanÄ±tlanamadÄ±');if(r.ok)load()}return <section className="panel health-section"><h2>{doctorMode?'Hasta sorularÄ±':'Uzmana sorularÄ±m'}</h2><p>Randevu iliÅŸkin bulunan uzmanla takip sorularÄ±nÄ± konu baÅŸlÄ±ÄŸÄ±yla yÃ¶net.</p>{!doctorMode&&<form className="question-form-v18" onSubmit={ask}><select name="doctorId" required><option value="">Uzman seÃ§</option>{doctors.map(d=><option key={d.id} value={d.id}>{d.name} Â· {d.specialty}</option>)}</select><input name="subject" placeholder="Konu" required/><textarea name="body" placeholder="Sorunu yaz" required/><button className="primary">Soruyu gÃ¶nder</button></form>}{msg&&<div className="inline-message">{msg}</div>}<div className="question-list-v18">{rows.map(q=><article key={q.id}><div className="row between"><div><b>{q.subject}</b><span>{doctorMode?q.patient?.name:q.doctor?.name} Â· {new Date(q.createdAt).toLocaleString('tr-TR')}</span></div><em>{q.status==='answered'?'YanÄ±tlandÄ±':q.status==='closed'?'KapalÄ±':'AÃ§Ä±k'}</em></div><p>{q.body}</p>{q.answer&&<div className="question-answer-v18"><strong>Uzman yanÄ±tÄ±</strong><p>{q.answer}</p></div>}{doctorMode&&q.status==='open'&&<form className="question-answer-form-v18" onSubmit={e=>answer(q.id,e)}><textarea name="answer" placeholder="YanÄ±t" required/><button className="primary">YanÄ±tla</button></form>}</article>)}{!rows.length&&<div className="empty">HenÃ¼z soru yok.</div>}</div></section>}

