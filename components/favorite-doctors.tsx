'use client';import {useEffect,useState} from 'react';
export default function FavoriteDoctors(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/doctors/favorites').then(r=>r.ok?r.json():[]).then(setRows)},[]);return <div className="doctor-list">{rows.map(d=><a className="doctor-card" href={`/doctors/${d.slug}`} key={d.id}><div className="avatar">{d.name.split(' ').slice(-2).map((x:string)=>x[0]).join('')}</div><div className="doctor-copy"><b>{d.name}</b><p>{d.specialty}</p><div className="meta"><span>★ {d.rating}</span>{d.organization?.name&&<span>{d.organization.name}</span>}</div></div></a>)}{!rows.length&&<div className="empty">Henüz favori doktorun yok.</div>}</div>}
