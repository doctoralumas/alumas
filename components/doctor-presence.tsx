"use client";import {useState} from 'react';const labels:any={OFFLINE:'Çevrimdışı',ONLINE:'Çevrimiçi',AVAILABLE:'Müsait',BUSY:'Meşgul'};export default function DoctorPresence({initial}:{initial:string}){const [status,setStatus]=useState(initial),[msg,setMsg]=useState('');async function save(v:string){setStatus(v);const r=await fetch('/api/doctor/presence',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({status:v})});setMsg(r.ok?'Durum güncellendi ✓':'Güncellenemedi')}return <div><div className="presence-picker-v25">{Object.keys(labels).map(k=><button key={k} className={status===k?'active':''} onClick={()=>save(k)}>{labels[k]}</button>)}</div>{msg&&<small>{msg}</small>}</div>}