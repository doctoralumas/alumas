"use client";import {labPanelTemplates} from "@/lib/lab-panels";
export default function LabPanelTemplates({onPick}:{onPick:(panel:string,test:string,unit?:string)=>void}){return <section className="panel health-section"><h2>Laboratuvar panel şablonları</h2><p>Panel seçerek sık kullanılan test adlarını hızlı doldur. Referans aralığını laboratuvar raporundaki değere göre ayrıca gir.</p><div className="lab-template-grid-v18">{labPanelTemplates.map(p=><article key={p.name}><b>{p.name}</b><span>{p.description}</span><div>{p.tests.map(t=><button className="secondary compact" key={t.name} onClick={()=>onPick(p.name,t.name,t.unit)}>{t.name}</button>)}</div></article>)}</div></section>}
