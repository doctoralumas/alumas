"use client";import {useState} from "react";export default function AccountControls(){const [confirm,setConfirm]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function del(){if(!window.confirm('Bu işlem geri alınamaz. Hesabı silmek istiyor musunuz?'))return;const r=await fetch('/api/account/delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirmation:confirm,password})});const d=await r.json();if(!r.ok)return setMsg(d.error||'Silinemedi');location.href='/'}return <div className="account-controls"><a className="btn secondary" href="/api/account/export">Verilerimi JSON olarak indir</a><div className="danger-zone"><b>Hesabı sil</b><p>Sağlık belgeleri dahil hesabına bağlı veriler silinir. Yasal saklama yükümlülükleri bulunan üretim senaryoları için ayrıca retention politikası tanımlanmalıdır.</p><input value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="HESABIMI SİL yazın"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Parola (varsa)" type="password"/><button className="btn danger" onClick={del}>Hesabımı kalıcı olarak sil</button>{msg&&<p>{msg}</p>}</div></div>}
