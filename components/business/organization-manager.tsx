"use client";import {useEffect,useState} from 'react';
type Props={org:any};const days=['Pazar','Pazartesi','Salı','Çarşamba','Perşembe','Cuma','Cumartesi'];
export default function OrganizationManager({org}:Props){const [services,setServices]=useState<any[]>(org.services||[]),[hours,setHours]=useState<any[]>(org.hours||[]),[invites,setInvites]=useState<any[]>(org.doctorInvites||[]),[msg,setMsg]=useState('');const [stock,setStock]=useState<any[]>(org.stocks||[]),[onDuty,setOnDuty]=useState(!!org.isOnDuty),[onDutyUntil,setOnDutyUntil]=useState(org.onDutyUntil?new Date(org.onDutyUntil).toISOString().slice(0,16):'');const [departments,setDepartments]=useState<any[]>(org.departments||[]),[campaigns,setCampaigns]=useState<any[]>(org.campaigns||[]),[doctors,setDoctors]=useState<any[]>(org.doctors||[]),[emergencyServices,setEmergencyServices]=useState<any[]>(org.emergencyServices||[]);

async function addDepartment(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/departments`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),floor:f.get('floor'),phone:f.get('phone'),description:f.get('description')})});const j=await r.json();if(r.ok){setDepartments(x=>[...x,j]);e.currentTarget.reset();setMsg('Departman eklendi ✓')}else setMsg(j.error)}
async function assignDepartment(doctorId:string,departmentId:string){const r=await fetch(`/api/organizations/${org.id}/doctor-department`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({doctorId,departmentId:departmentId||null})});if(r.ok){setDoctors(x=>x.map(d=>d.id===doctorId?{...d,departmentId:departmentId||null}:d));setMsg('Doktor departmanı güncellendi ✓')}else setMsg('Departman atanamadı')}
async function addCampaign(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/campaigns`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title:f.get('title'),description:f.get('description'),startsAt:f.get('startsAt')||null,endsAt:f.get('endsAt')||null})});const j=await r.json();if(r.ok){setCampaigns(x=>[j,...x]);e.currentTarget.reset();setMsg('Kampanya eklendi ✓')}else setMsg(j.error)}
async function addVariant(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/service-variants`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({serviceId:f.get("serviceId"),label:f.get("label"),priceDelta:f.get("priceDelta"),durationMinutes:f.get("durationMinutes")})});const j=await r.json();if(r.ok){setServices(x=>x.map(s=>s.id===j.serviceId?{...s,variants:[...(s.variants||[]),j]}:s));e.currentTarget.reset();setMsg("Varyant eklendi ✓")}else setMsg(j.error)}
async function addEmergencyService(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/emergency-services`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),kind:f.get("kind"),phone:f.get("phone"),is24Hours:f.get("is24Hours")==="on",description:f.get("description")})});const j=await r.json();if(r.ok){setEmergencyServices(x=>[...x,j]);e.currentTarget.reset();setMsg("Acil hizmet eklendi ✓")}else setMsg(j.error)}
async function addService(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/services`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),description:f.get('description'),price:f.get('price')})});const j=await r.json();if(r.ok){setServices(x=>[...x,j]);e.currentTarget.reset();setMsg('Hizmet eklendi ✓')}else setMsg(j.error)}
async function invite(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/invites`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),specialty:f.get('specialty')})});const j=await r.json();if(r.ok){setInvites(x=>[j,...x]);e.currentTarget.reset();setMsg('Doktor daveti gönderildi ✓')}else setMsg(j.error)}
async function saveHours(){const payload=days.map((_,weekday)=>{const row=hours.find((h:any)=>h.weekday===weekday)||{};return {weekday,isClosed:!!row.isClosed,opensAt:row.opensAt||'09:00',closesAt:row.closesAt||'18:00'}});const r=await fetch(`/api/organizations/${org.id}/hours`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({hours:payload})});setMsg(r.ok?'Çalışma saatleri kaydedildi ✓':'Saatler kaydedilemedi')}
function updateHour(day:number,key:string,value:any){setHours((prev:any[])=>{const rest=prev.filter(h=>h.weekday!==day);const old=prev.find(h=>h.weekday===day)||{weekday:day,opensAt:'09:00',closesAt:'18:00',isClosed:false};return [...rest,{...old,[key]:value}]})}
async function locate(){setMsg('Konum aranıyor...');const r=await fetch('/api/maps/geocode',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query:`${org.address}, ${org.district||''}, ${org.city}, Türkiye`})});const j=await r.json();if(!r.ok){setMsg(j.error||'Konum bulunamadı');return}const u=await fetch(`/api/organizations/${org.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(j)});setMsg(u.ok?'Harita konumu kaydedildi ✓':'Konum kaydedilemedi')}
async function saveDuty(){const r=await fetch(`/api/organizations/${org.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({isOnDuty:onDuty,onDutyUntil:onDuty&&onDutyUntil?new Date(onDutyUntil).toISOString():null})});setMsg(r.ok?'Nöbetçi durumu güncellendi ✓':'Nöbetçi durumu kaydedilemedi')}
async function addStock(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`/api/organizations/${org.id}/stock`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({itemName:f.get('itemName'),stockStatus:f.get('stockStatus'),quantity:f.get('quantity')})});const j=await r.json();if(r.ok){setStock(x=>[...x.filter(s=>s.itemName!==j.itemName),j]);e.currentTarget.reset();setMsg('Stok güncellendi ✓')}else setMsg(j.error)}
return <div className="business-manage-grid"><section className="panel form-span"><div className="row between"><div><h2>Konum</h2><p>{org.address} · {org.city}</p></div><button className="secondary" onClick={locate}>Adresten harita konumu bul</button></div><small className="muted">Mapbox anahtarı tanımlıysa koordinat otomatik kaydedilir.</small></section>{msg&&<div className="inline-message form-span">{msg}</div>}<section className="panel"><h2>Hizmetler ve fiyatlar</h2><form className="compact-form" onSubmit={addService}><input name="name" placeholder="Hizmet adı" required/><input name="price" type="number" min="0" placeholder="Fiyat (₺)"/><input name="description" placeholder="Kısa açıklama"/><button className="primary">Ekle</button></form><div className="slot-list">{services.map(s=><div className="slot-row" key={s.id}><div><b>{s.name}</b><span>{s.description||'Açıklama yok'}</span></div><strong>{s.price?`${s.price.toLocaleString('tr-TR')} ₺`:'Fiyat sorunuz'}</strong></div>)}</div></section><section className="panel"><h2>Hizmet varyantları</h2><form className="compact-form" onSubmit={addVariant}><select name="serviceId" required><option value="">Hizmet seç</option>{services.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input name="label" placeholder="Online / 60 dk / Premium" required/><input name="priceDelta" type="number" placeholder="Ek fiyat (₺)"/><input name="durationMinutes" type="number" placeholder="Süre (dk)"/><button className="primary">Varyant ekle</button></form><div className="slot-list">{services.flatMap(s=>(s.variants||[]).map((v:any)=><div className="slot-row" key={v.id}><div><b>{s.name} · {v.label}</b><span>{v.durationMinutes?`${v.durationMinutes} dk`:''}{v.priceDelta?` · +${v.priceDelta} ₺`:''}</span></div></div>))}</div></section><section className="panel"><h2>Doktor davetleri</h2><form className="compact-form" onSubmit={invite}><input name="email" type="email" placeholder="doktor@ornek.com" required/><input name="specialty" placeholder="Branş (opsiyonel)"/><button className="primary">Davet gönder</button></form><div className="slot-list">{invites.map(i=><div className="slot-row" key={i.id}><div><b>{i.email}</b><span>{i.specialty||'Doktor'} · {i.status}</span></div></div>)}</div></section><section className="panel form-span"><div className="row between"><div><h2>Çalışma saatleri</h2><p>Kuruma göre haftalık açılış/kapanış bilgisi.</p></div><button className="secondary" onClick={saveHours}>Kaydet</button></div><div className="hours-grid">{days.map((d,weekday)=>{const h=hours.find((x:any)=>x.weekday===weekday)||{opensAt:'09:00',closesAt:'18:00',isClosed:false};return <div className="hours-row" key={d}><b>{d}</b><label><input type="checkbox" checked={!!h.isClosed} onChange={e=>updateHour(weekday,'isClosed',e.target.checked)}/> Kapalı</label><input type="time" disabled={h.isClosed} value={h.opensAt||'09:00'} onChange={e=>updateHour(weekday,'opensAt',e.target.value)}/><input type="time" disabled={h.isClosed} value={h.closesAt||'18:00'} onChange={e=>updateHour(weekday,'closesAt',e.target.value)}/></div>})}</div></section><section className="panel form-span"><h2>Branşlar / Departmanlar</h2><p>Özellikle hastane ve kliniklerde doktorları departmanlara bağla.</p><form className="compact-form" onSubmit={addDepartment}><input name="name" placeholder="Kardiyoloji" required/><input name="floor" placeholder="Kat / bölüm"/><input name="phone" placeholder="Dahili telefon"/><input name="description" placeholder="Kısa açıklama"/><button className="primary">Departman ekle</button></form><div className="v25-department-grid">{departments.map(d=><article key={d.id}><b>{d.name}</b><small>{d.floor||'Kat belirtilmedi'} {d.phone?`· ${d.phone}`:''}</small></article>)}</div>{doctors.length>0&&<div className="slot-list">{doctors.map(d=><div className="slot-row" key={d.id}><div><b>{d.name}</b><span>{d.specialty}</span></div><select value={d.departmentId||''} onChange={e=>assignDepartment(d.id,e.target.value)}><option value="">Departmansız</option>{departments.map(dep=><option value={dep.id} key={dep.id}>{dep.name}</option>)}</select></div>)}</div>}</section><section className="panel form-span"><h2>Kampanyalar</h2><p>Onaylı kurum profilinde ve Alumas Kampanyalar sayfasında gösterilir.</p><form className="v25-campaign-form" onSubmit={addCampaign}><input name="title" placeholder="Kampanya başlığı" required/><input name="startsAt" type="date"/><input name="endsAt" type="date"/><textarea name="description" placeholder="Kısa açıklama"/><button className="primary">Yayınla</button></form><div className="slot-list">{campaigns.map(c=><div className="slot-row" key={c.id}><div><b>{c.title}</b><span>{c.description||'Açıklama yok'}</span></div><small>{c.isActive?'Aktif':'Pasif'}</small></div>)}</div></section>{org.type!=='PHARMACY'&&<section className="panel form-span"><h2>Acil hizmetler</h2><p>Hastane/klinik acil hizmetleri, danışma hattı veya 7/24 birimler.</p><form className="compact-form" onSubmit={addEmergencyService}><input name="name" placeholder="Acil Servis" required/><select name="kind"><option value="EMERGENCY_DEPARTMENT">Acil servis</option><option value="EMERGENCY_CONSULT">Acil danışmanlık</option><option value="AMBULANCE_COORDINATION">Ambulans koordinasyonu</option></select><input name="phone" placeholder="Telefon"/><label className="row"><input name="is24Hours" type="checkbox"/> 7/24</label><input name="description" placeholder="Açıklama"/><button className="primary">Ekle</button></form><div className="slot-list">{emergencyServices.map((e:any)=><div className="slot-row" key={e.id}><div><b>{e.name}</b><span>{e.kind}{e.is24Hours?' · 7/24':''}</span></div></div>)}</div></section>}{org.type==='PHARMACY'&&<><section className="panel form-span"><div className="row between"><div><h2>Nöbetçi eczane</h2><p>Bu bilgi kullanıcıların nöbetçi eczane filtresinde görünür.</p></div><button className="secondary" onClick={saveDuty}>Kaydet</button></div><div className="compact-form"><label className="row"><input type="checkbox" checked={onDuty} onChange={e=>setOnDuty(e.target.checked)}/> Nöbetçi</label><input type="datetime-local" disabled={!onDuty} value={onDutyUntil} onChange={e=>setOnDutyUntil(e.target.value)}/></div></section><section className="panel form-span"><h2>Eczane stok yönetimi</h2><p>Kullanıcılara adet yerine yalnızca stok durumu gösterilir.</p><form className="compact-form" onSubmit={addStock}><input name="itemName" placeholder="Ürün / ilaç adı" required/><select name="stockStatus"><option value="in_stock">Stokta var</option><option value="limited">Sınırlı</option><option value="out_of_stock">Yok</option></select><input name="quantity" type="number" min="0" placeholder="İç adet (özel)"/><button className="primary">Güncelle</button></form><div className="slot-list">{stock.map(s=><div className="slot-row" key={s.id}><b>{s.itemName}</b><span>{s.stockStatus}</span></div>)}</div></section></>}</div>}
