"use client";import {useEffect,useState} from "react";export default function ReleaseDashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch("/api/admin/release-status").then(r=>r.json()).then(setD)},[]);if(!d)return <div className="panel">Release durumu yükleniyor…</div>;const go=d.databaseReady&&!d.maintenance;return <div className="profile-columns"><section className="panel"><span className="kicker">Go / No-Go</span><h2>{go?"GO ✓":"NO-GO"}</h2><p>DB: {d.databaseReady?"Hazır":"Hazır değil"} · Bakım modu: {d.maintenance?"Açık":"Kapalı"}</p></section><section className="panel"><h2>Build</h2><p>Sürüm: <b>{d.version}</b></p><p>SHA: <code>{d.buildSha}</code></p><p>Kanal: {d.channel}</p></section><section className="panel form-span"><h2>Feature flags</h2><div className="slot-list">{Object.entries(d.features||{}).map(([k,v])=><div className="slot-row" key={k}><b>{k}</b><span className="status">{v?"Açık":"Kapalı"}</span></div>)}</div></section></div>}
