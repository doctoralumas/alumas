'use client';import {useEffect,useState} from 'react';
export default function FavoriteOrganizations(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/organizations/favorites').then(r=>r.ok?r.json():[]).then(setRows)},[]);return <div className="organization-grid">{rows.map(o=><a className="organization-card" href={`/organizations/${o.slug}`} key={o.id}><div className={`org-logo ${o.type.toLowerCase()}`}>{o.name[0]}</div><div><span className="kicker">Favori kurum</span><h3>{o.name}</h3><p>{o.city}{o.district?` · ${o.district}`:''}</p></div></a>)}{!rows.length&&<div className="empty">Henüz favori kurumun yok.</div>}</div>}
