"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function DoctorOnboardingForm(){const [msg,setMsg]=useState("");const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg("Gönderiliyor…");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/onboarding/doctor",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const j=await res.json();if(!res.ok){setMsg(j.error||"Profil oluşturulamadı");return}setMsg("Profil oluşturuldu. Doğrulama sonrası halka açık listede yayınlanacak.");setTimeout(()=>r.push("/doctor"),700)}return <form className="business-form" onSubmit={submit}><label>Unvan<input name="title" placeholder="Uzm. Dr." required/></label><label>Uzmanlık alanı<input name="specialty" placeholder="İç Hastalıkları" required/></label><div className="form-pair"><label>Şehir<input name="city" required/></label><label>Çalıştığı kurum / bağımsız<input name="hospital" placeholder="Kurum adı veya Bağımsız" required/></label></div><label>Mesleki sicil / ruhsat no<input name="licenseNumber" required/></label><label>Kısa biyografi<textarea name="bio" rows={5} required/></label><label>Görüşme başlangıç fiyatı (₺)<input name="price" type="number" min="0" defaultValue="0"/></label>{msg&&<div className="inline-message">{msg}</div>}<button className="primary">Doktor profilimi oluştur</button></form>}