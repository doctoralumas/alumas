"use client";import {useEffect,useState} from "react";export default function ContentAdminClient(){const [d,setD]=useState<any>({banners:[],categories:[],inquiries:[]}),[msg,setMsg]=useState("");const load=()=>fetch("/api/admin/content").then(r=>r.json()).then(setD);useEffect(()=>{load()},[]);async function create(e:any,kind:string){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/content",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,...Object.fromEntries(f)})});setMsg(r.ok?"Kaydedildi ✓":"Kaydedilemedi");if(r.ok){e.currentTarget.reset();load()}}async function toggle(kind:string,id:string,isActive:boolean){await fetch("/api/admin/content",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({kind,id,isActive})});load()}return <div className="business-manage-grid">{msg&&<div className="inline-message form-span">{msg}</div>}<section className="panel"><h2>Ana sayfa banner / slider</h2><form className="compact-form" onSubmit={e=>create(e,"banner")}><input name="title" placeholder="Başlık" required/><input name="subtitle" placeholder="Alt metin"/><input name="ctaLabel" placeholder="Buton etiketi"/><input name="ctaHref" placeholder="/doctors"/><input name="background" placeholder="#DED7FF"/><input name="sortOrder" type="number" defaultValue="0"/><button className="primary">Banner ekle</button></form><div className="slot-list">{d.banners.map((x:any)=><div className="slot-row" key={x.id}><div><b>{x.title}</b><span>{x.subtitle||""}</span></div><button className="secondary" onClick={()=>toggle("banner",x.id,!x.isActive)}>{x.isActive?"Pasifleştir":"Aktifleştir"}</button></div>)}</div></section><section className="panel"><h2>Kategori / şablon yönetimi</h2><form className="compact-form" onSubmit={e=>create(e,"category")}><input name="name" placeholder="Kategori adı" required/><input name="slug" placeholder="slug"/><select name="categoryKind"><option value="service">Hizmet</option><option value="content">İçerik</option><option value="tourism">Sağlık turizmi</option></select><input name="templateKey" placeholder="Şablon" defaultValue="default"/><input name="description" placeholder="Açıklama"/><button className="primary">Kategori ekle</button></form><div className="slot-list">{d.categories.map((x:any)=><div className="slot-row" key={x.id}><div><b>{x.name}</b><span>{x.kind} · {x.templateKey}</span></div><button className="secondary" onClick={()=>toggle("category",x.id,!x.isActive)}>{x.isActive?"Pasifleştir":"Aktifleştir"}</button></div>)}</div></section><section className="panel form-span"><h2>İletişim talepleri</h2><div className="slot-list">{d.inquiries.map((x:any)=><div className="slot-row" key={x.id}><div><b>{x.subject}</b><span>{x.name} · {x.email} · {x.message}</span></div><span className="status">{x.status}</span></div>)}</div></section></div>}