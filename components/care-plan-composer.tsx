"use client";import {useState} from "react";export default function CarePlanComposer({patients}:{patients:{id:string;name:string}[]}){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/care-plans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({patientId:f.get('patientId'),title:f.get('title'),summary:f.get('summary'),items:[{kind:'instruction',title:f.get('itemTitle'),instructions:f.get('instructions'),schedule:f.get('schedule')}]})});const j=await r.json();setMsg(r.ok?'Bakım planı hastaya gönderildi.':j.error||'Kaydedilemedi');if(r.ok)e.currentTarget.reset()}return <form className="care-form" onSubmit={submit}><select name="patientId" required><option value="">Hasta seç</option>{patients.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="title" placeholder="Plan başlığı" required/><textarea name="summary" placeholder="Kısa açıklama"/><input name="itemTitle" placeholder="Örn. İlacı kullan / yürüyüş" required/><input name="instructions" placeholder="Talimat"/><input name="schedule" placeholder="Örn. Günde 2 kez"/><button className="primary">Planı gönder</button>{msg&&<div className="inline-message form-span">{msg}</div>}</form>}