"use client";import {useState} from "react";export default function CarePlanComposer({patients}:{patients:{id:string;name:string}[]}){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch('/api/care-plans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({patientId:f.get('patientId'),title:f.get('title'),summary:f.get('summary'),items:[{kind:'instruction',title:f.get('itemTitle'),instructions:f.get('instructions'),schedule:f.get('schedule')}]})});const j=await r.json();setMsg(r.ok?'BakÄ±m planÄ± hastaya gÃ¶nderildi.':j.error||'Kaydedilemedi');if(r.ok)form.reset()}return <form className="care-form" onSubmit={submit}><select name="patientId" required><option value="">Hasta seÃ§</option>{patients.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="title" placeholder="Plan baÅŸlÄ±ÄŸÄ±" required/><textarea name="summary" placeholder="KÄ±sa aÃ§Ä±klama"/><input name="itemTitle" placeholder="Ã–rn. Ä°lacÄ± kullan / yÃ¼rÃ¼yÃ¼ÅŸ" required/><input name="instructions" placeholder="Talimat"/><input name="schedule" placeholder="Ã–rn. GÃ¼nde 2 kez"/><button className="primary">PlanÄ± gÃ¶nder</button>{msg&&<div className="inline-message form-span">{msg}</div>}</form>}
