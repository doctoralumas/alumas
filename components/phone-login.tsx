"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function PhoneLogin(){const r=useRouter(),[phone,setPhone]=useState(""),[code,setCode]=useState(""),[step,setStep]=useState<"phone"|"code">("phone"),[msg,setMsg]=useState("");async function request(){setMsg("");const x=await fetch("/api/auth/otp/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone})});const j=await x.json();if(!x.ok)return setMsg(j.error||"Kod gönderilemedi");setStep("code");setMsg(j.devCode?`Geliştirme kodu: ${j.devCode}`:"Doğrulama kodu gönderildi.")}async function verify(){const x=await fetch("/api/auth/otp/verify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone,code,login:true})});const j=await x.json();if(!x.ok)return setMsg(j.error||"Kod doğrulanamadı");r.push("/");r.refresh()}return <div className="auth-card"><span className="kicker">Telefonla giriş</span><h1>Alumas’a gir</h1><label>Telefon<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+90 5xx xxx xx xx" disabled={step==='code'}/></label>{step==='code'&&<label>6 haneli kod<input value={code} onChange={e=>setCode(e.target.value)} inputMode="numeric" maxLength={6} placeholder="000000"/></label>}{msg&&<div className="inline-message">{msg}</div>}<button className="primary full" onClick={step==='phone'?request:verify}>{step==='phone'?"Kod gönder":"Giriş yap"}</button>{step==='code'&&<button className="secondary full auth-secondary" onClick={()=>setStep('phone')}>Telefonu değiştir</button>}</div>}