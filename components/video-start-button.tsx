"use client";import {useState} from "react";export default function VideoStartButton({appointmentId}:{appointmentId:string}){const [busy,setBusy]=useState(false);async function start(){setBusy(true);const r=await fetch('/api/video-consultations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({appointmentId})});const j=await r.json();setBusy(false);if(r.ok)location.href=j.joinUrl;else alert(j.error||'Görüşme başlatılamadı')}return <button className="secondary" onClick={start} disabled={busy}>{busy?'Hazırlanıyor…':'Görüntülü görüşme'}</button>}
