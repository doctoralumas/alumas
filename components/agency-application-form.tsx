"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function AgencyApplicationForm(){const [msg,setMsg]=useState("");const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/agencies",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(b)});const j=await res.json();if(!res.ok){setMsg(j.error||"Başvuru gönderilemedi");return}setMsg("Acente profiliniz oluşturuldu ve doğrulamaya gönderildi.");setTimeout(()=>r.push(`/agency/${j.id}`),700)}return <form className="business-form" onSubmit={submit}><label>Acente adı<input name="name" required/></label><div className="form-pair"><label>Şehir<input name="city" required/></label><label>Telefon<input name="phone" required/></label></div><label>E-posta<input name="email" type="email" required/></label><label>Web sitesi<input name="website" placeholder="https://"/></label><label>Desteklenen diller<input name="languages" placeholder="Türkçe, English, Deutsch"/></label><label>Açıklama<textarea name="description" rows={5} required/></label>{msg&&<div className="inline-message">{msg}</div>}<button className="primary">Acente başvurusu oluştur</button></form>}