"use client";
import SectionVisual from "@/components/section-visual";import {useEffect,useState} from "react";type R={id:string;serviceType:string;city:string;district?:string;status:string;preferredAt?:string};const services=['Evde doktor ziyareti','Evde hemşirelik','Kan / numune alma','Pansuman','Fizyoterapi desteği'];export default function Page(){const [rows,setRows]=useState<R[]>([]),[msg,setMsg]=useState('');const load=()=>fetch('/api/home-care').then(r=>r.json()).then(x=>setRows(Array.isArray(x)?x:[]));useEffect(load,[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),b=Object.fromEntries(f.entries());const r=await fetch('/api/home-care',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});setMsg(r.ok?'Talep alındı. Uygun sağlayıcı eşleşmesi yapılacak.':'Talep oluşturulamadı');if(r.ok)load()}return <div className="page"><SectionVisual slug="home-care" alt="Evde sağlık"/><div className="page-title"><span className="kicker">Alumas Care</span><h1>Evde Sağlık</h1><p>Evde sunulabilecek sağlık hizmetleri için talep oluştur. Acil durumlarda bu modülü kullanma.</p></div><div className="v24-service-grid">{services.map((s,i)=><article className={['purple-v14','mint-v14','peach-v14','sky-v14','lime-v14'][i]} key={s}><b>{s}</b><small>Talep ve sağlayıcı eşleşmesi</small></article>)}</div><form className="panel v24-form" onSubmit={submit}><select name="serviceType" required>{services.map(s=><option key={s}>{s}</option>)}</select><input name="city" placeholder="Şehir" required/><input name="district" placeholder="İlçe"/><input name="preferredAt" type="datetime-local"/><textarea name="addressNote" placeholder="Adres / konum notu"/><textarea name="note" placeholder="Hizmet notu"/><button className="primary">Talep oluştur</button>{msg&&<span>{msg}</span>}</form><section className="panel"><h2>Taleplerim</h2>{rows.map(r=><div className="share-row" key={r.id}><div><b>{r.serviceType}</b><small>{r.city}{r.district?` / ${r.district}`:''}</small></div><span className="status">{r.status}</span></div>)}</section></div>}
