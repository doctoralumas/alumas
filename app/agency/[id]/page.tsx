import {redirect,notFound} from "next/navigation";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";import AgencyManager from "@/components/agency-manager";export default async function Page({params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)redirect("/login");const {id}=await params;const a=await prisma.healthTourismAgency.findFirst({where:u.role==="ADMIN"?{id}:{id,ownerUserId:u.id},include:{services:true,packages:true}});if(!a)notFound();return <div className="page"><div className="page-title"><span className="kicker">Acente paneli</span><h1>{a.name}</h1><p>{a.status} · {a.isVerified?"Doğrulandı":"Doğrulama bekliyor"}</p></div><AgencyManager agency={a}/><section className="panel health-section"><h2>Mevcut hizmetler ve paketler</h2><div className="slot-list">{a.services.map(x=><div className="slot-row" key={x.id}><div><b>{x.title}</b><span>{x.kind}</span></div></div>)}{a.packages.map(x=><div className="slot-row" key={x.id}><div><b>{x.title}</b><span>{x.category} · {x.city}</span></div><span className="status">{x.isPublished?"YAYINDA":"TASLAK"}</span></div>)}</div></section></div>}