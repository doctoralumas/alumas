import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import DoctorOnboardingForm from "@/components/doctor-onboarding-form";export default async function Page(){const u=await currentUser();if(!u)redirect("/login");if(u.doctorProfile)redirect("/doctor");return <div className="page"><div className="page-title"><span className="kicker">Alumas Pro</span><h1>Doktor profilini oluştur</h1><p>Profilin doğrulama tamamlanana kadar sadece senin panelinde görünür.</p></div><section className="panel"><DoctorOnboardingForm/></section></div>}