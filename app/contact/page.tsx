"use client";import {useState} from "react";export default function ContactPage(){const [msg,setMsg]=useState("");async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const j=await r.json();setMsg(r.ok?"Mesajın alındı ✓":j.error||"Gönderilemedi");if(r.ok)e.currentTarget.reset()}return <div className="page"><div className="page-title"><span className="kicker">Destek</span><h1>İletişim</h1><p>Alumas ile ilgili ürün, kurum hesabı veya destek taleplerini iletebilirsin.</p></div><section className="panel"><form className="v26-contact-form" onSubmit={submit}><input name="name" placeholder="Ad soyad" required/><input name="email" type="email" placeholder="E-posta" required/><input name="phone" placeholder="Telefon (opsiyonel)"/><input name="subject" placeholder="Konu" required/><textarea name="message" placeholder="Mesajın" required/><button className="primary">Gönder</button></form>{msg&&<div className="inline-message">{msg}</div>}</section><section className="panel health-section"><h2>Acil durumlar</h2><p>Bu iletişim formu acil yardım kanalı değildir. Türkiye’de acil yardım için 112’yi arayın.</p><a className="secondary" href="tel:112">112’yi ara</a></section></div>}