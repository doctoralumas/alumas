import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";
export async function GET(req:Request){const u=new URL(req.url);const q=(u.searchParams.get('q')||'').trim();const city=(u.searchParams.get('city')||'').trim();if(q.length<2)return NextResponse.json([]);const rows=await prisma.pharmacyStock.findMany({where:{itemName:{contains:q,mode:'insensitive'},organization:{type:'PHARMACY',status:'APPROVED',isPublished:true,...(city?{city:{equals:city,mode:'insensitive'}}:{})}},include:{organization:{select:{id:true,slug:true,name:true,city:true,district:true,address:true,phone:true,latitude:true,longitude:true}}},take:25,orderBy:{updatedAt:'desc'}});return NextResponse.json(rows.map(r=>({...r,quantity:undefined}))) }
