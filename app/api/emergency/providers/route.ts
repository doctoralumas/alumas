import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";function km(a:number,b:number,c:number,d:number){const R=6371,p=(x:number)=>x*Math.PI/180;const z=Math.sin(p(c-a)/2)**2+Math.cos(p(a))*Math.cos(p(c))*Math.sin(p(d-b)/2)**2;return 2*R*Math.asin(Math.sqrt(z))}export async function GET(req:Request){const u=new URL(req.url),kind=u.searchParams.get("kind"),lat=Number(u.searchParams.get("lat")),lng=Number(u.searchParams.get("lng"));let rows=await prisma.emergencyProvider.findMany({where:{isActive:true,...(kind?{kind}:{})},orderBy:[{isVerified:"desc"},{name:"asc"}]});if(Number.isFinite(lat)&&Number.isFinite(lng))rows=rows.map((x:any)=>({...x,distanceKm:x.latitude!=null&&x.longitude!=null?km(lat,lng,x.latitude,x.longitude):null})).sort((a:any,b:any)=>(a.distanceKm??99999)-(b.distanceKm??99999));return NextResponse.json(rows)}