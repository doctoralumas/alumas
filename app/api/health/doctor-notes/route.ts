import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});return NextResponse.json(await prisma.doctorPatientNote.findMany({where:{patientId:u.id,patientVisible:true},include:{doctor:{select:{name:true}}},orderBy:{createdAt:'desc'}}))}