import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export async function GET(){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});return NextResponse.json(await prisma.specialHealthProfile.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}}))}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});const b=await req.json();const type=String(b.type||'');if(!['PREGNANCY','CHILD'].includes(type)||!b.name)return NextResponse.json({error:'Profil türü ve adı gerekli'},{status:400});const row=await prisma.specialHealthProfile.create({data:{userId:u.id,type:type as any,name:String(b.name).slice(0,120),relationLabel:b.relationLabel?String(b.relationLabel).slice(0,80):null,birthDate:b.birthDate?new Date(b.birthDate):null,dueDate:b.dueDate?new Date(b.dueDate):null,startDate:b.startDate?new Date(b.startDate):null,notes:b.notes?String(b.notes).slice(0,1000):null}});return NextResponse.json(row,{status:201})}
export async function PATCH(req:Request){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:'Erişim yok'},{status:403});const b=await req.json();const row=await prisma.specialHealthProfile.findFirst({where:{id:String(b.id||''),userId:u.id}});if(!row)return NextResponse.json({error:'Profil bulunamadı'},{status:404});return NextResponse.json(await prisma.specialHealthProfile.update({where:{id:row.id},data:{isActive:b.isActive===undefined?row.isActive:Boolean(b.isActive),notes:b.notes===undefined?row.notes:String(b.notes).slice(0,1000)}}))}
