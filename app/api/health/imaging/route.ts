import {NextResponse} from 'next/server';import {currentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {storePrivateFile} from '@/lib/storage';
async function doctorAccess(patientId:string,doctorId:string){const [c,s,r]=await Promise.all([prisma.healthShareConsent.findUnique({where:{patientId_doctorId:{patientId,doctorId}}}),prisma.healthReportShare.findFirst({where:{patientId,doctorId,status:'active',OR:[{expiresAt:null},{expiresAt:{gt:new Date()}}]}}),prisma.appointment.findFirst({where:{userId:patientId,doctorId}})]);return !!r&&((c?.status==='active'&&c.scopes.includes('imaging'))||!!s)}
export async function GET(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const q=new URL(req.url).searchParams;let userId=u.id;if(u.role==='DOCTOR'&&u.doctorProfile){const p=String(q.get('patientId')||'');if(!p||!(await doctorAccess(p,u.doctorProfile.id)))return NextResponse.json({error:'Erişim yok'},{status:403});userId=p}else if(u.role!=='PATIENT')return NextResponse.json({error:'Erişim yok'},{status:403});return NextResponse.json(await prisma.imagingResult.findMany({where:{userId},orderBy:{performedAt:'desc'}}))}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=='PATIENT')return NextResponse.json({error:'Hasta hesabı gerekli'},{status:403});const f=await req.formData(),title=String(f.get('title')||'').trim(),modality=String(f.get('modality')||'').trim();if(!title||!modality)return NextResponse.json({error:'Başlık ve görüntüleme türü gerekli'},{status:400});let fileName=null,mimeType=null,storagePath=null,sizeBytes=null;const file=f.get('file');if(file instanceof File&&file.size){if(file.size>8*1024*1024)return NextResponse.json({error:'Dosya 8 MB sınırını aşıyor'},{status:413});if(!['application/pdf','image/jpeg','image/png'].includes(file.type))return NextResponse.json({error:'PDF, JPG veya PNG yükleyin'},{status:415});storagePath=await storePrivateFile(file,u.id);fileName=file.name;mimeType=file.type;sizeBytes=file.size}const row=await prisma.imagingResult.create({data:{userId:u.id,title:title.slice(0,160),modality:modality.slice(0,60),bodyPart:String(f.get('bodyPart')||'').slice(0,100)||null,provider:String(f.get('provider')||'').slice(0,140)||null,reportText:String(f.get('reportText')||'').slice(0,5000)||null,impression:String(f.get('impression')||'').slice(0,2000)||null,performedAt:f.get('performedAt')?new Date(String(f.get('performedAt'))):new Date(),fileName,mimeType,storagePath,sizeBytes}});return NextResponse.json(row,{status:201})}
