import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});return NextResponse.json(await prisma.healthReportShare.findMany({where:{patientId:u.id,status:"active"},include:{doctor:{select:{name:true,specialty:true}}},orderBy:{createdAt:"desc"}}));}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});const b=await req.json();const doctorId=String(b.doctorId||"");const fromDate=new Date(b.fromDate),toDate=new Date(b.toDate),expiresAt=b.expiresAt?new Date(b.expiresAt):null;if(!doctorId||!Number.isFinite(fromDate.getTime())||!Number.isFinite(toDate.getTime())||fromDate>toDate)return NextResponse.json({error:"Bilgileri kontrol edin"},{status:400});const relation=await prisma.appointment.findFirst({where:{userId:u.id,doctorId,status:{not:"cancelled"}}});if(!relation)return NextResponse.json({error:"Bu uzmanla randevu ilişkin bulunmuyor"},{status:403});const row=await prisma.healthReportShare.create({data:{patientId:u.id,doctorId,fromDate,toDate,expiresAt}});return NextResponse.json(row,{status:201});}
export async function DELETE(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"id gerekli"},{status:400});await prisma.healthReportShare.updateMany({where:{id,patientId:u.id},data:{status:"revoked"}});return NextResponse.json({ok:true});}
