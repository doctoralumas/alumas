import {NextResponse} from "next/server";
import {currentUser} from "@/lib/auth";
import {prisma} from "@/lib/prisma";
const num=(v:any)=>{if(v===null||v===undefined||v==='')return null;const n=Number(String(v).replace(',','.'));return Number.isFinite(n)?n:null};
const norm=(s:string)=>s.trim().toLocaleLowerCase('tr-TR').replace(/\s+/g,' ');
async function doctorPatientAccess(patientId:string,doctorId:string){const [consent,share,rel]=await Promise.all([prisma.healthShareConsent.findUnique({where:{patientId_doctorId:{patientId,doctorId}}}),prisma.healthReportShare.findFirst({where:{patientId,doctorId,status:'active',OR:[{expiresAt:null},{expiresAt:{gt:new Date()}}]}}),prisma.appointment.findFirst({where:{userId:patientId,doctorId}})]);return Boolean(rel&&((consent?.status==='active'&&consent.scopes.includes('labs'))||share))}
export async function GET(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const url=new URL(req.url),test=url.searchParams.get('test'),from=url.searchParams.get('from'),to=url.searchParams.get('to');let userId=u.id;if(u.role==='DOCTOR'&&u.doctorProfile){const patientId=String(url.searchParams.get('patientId')||'');if(!patientId||!(await doctorPatientAccess(patientId,u.doctorProfile.id)))return NextResponse.json({error:'Erişim yok'},{status:403});userId=patientId}else if(u.role!=='PATIENT')return NextResponse.json({error:'Erişim yok'},{status:403});const where:any={userId};if(test)where.normalizedName=norm(test);if(from||to)where.measuredAt={...(from?{gte:new Date(`${from}T00:00:00`)}:{}),...(to?{lte:new Date(`${to}T23:59:59.999`)}:{})};return NextResponse.json(await prisma.labResult.findMany({where,orderBy:{measuredAt:"desc"}}))}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=='PATIENT')return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});const b=await req.json();if(!b.testName||b.value===undefined)return NextResponse.json({error:"Test adı ve sonuç gerekli"},{status:400});const numericValue=num(b.numericValue??b.value),referenceLow=num(b.referenceLow),referenceHigh=num(b.referenceHigh);let status=String(b.status||'normal');if(numericValue!=null){if(referenceLow!=null&&numericValue<referenceLow)status='low';else if(referenceHigh!=null&&numericValue>referenceHigh)status='high';else if(referenceLow!=null||referenceHigh!=null)status='normal'}const row=await prisma.labResult.create({data:{userId:u.id,testName:String(b.testName).slice(0,120),normalizedName:norm(String(b.testName)).slice(0,120),panel:b.panel?String(b.panel).slice(0,120):null,value:String(b.value).slice(0,80),numericValue,unit:b.unit?String(b.unit).slice(0,40):null,reference:b.reference?String(b.reference).slice(0,100):null,referenceLow,referenceHigh,status,note:b.note?String(b.note).slice(0,500):null,measuredAt:b.measuredAt?new Date(b.measuredAt):new Date()}});return NextResponse.json(row,{status:201})}
