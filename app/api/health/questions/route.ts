import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export async function GET(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const url=new URL(req.url);if(u.role==='PATIENT')return NextResponse.json(await prisma.patientDoctorQuestion.findMany({where:{patientId:u.id},include:{doctor:{select:{name:true,specialty:true}}},orderBy:{createdAt:'desc'}}));if(u.role==='DOCTOR'&&u.doctorProfile){const patientId=url.searchParams.get('patientId');return NextResponse.json(await prisma.patientDoctorQuestion.findMany({where:{doctorId:u.doctorProfile.id,...(patientId?{patientId}: {})},include:{patient:{select:{name:true}}},orderBy:[{status:'asc'},{createdAt:'desc'}]}))}return NextResponse.json({error:'Erişim yok'},{status:403})}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=='PATIENT')return NextResponse.json({error:'Hasta hesabı gerekli'},{status:403});const b=await req.json(),doctorId=String(b.doctorId||''),subject=String(b.subject||'').trim(),body=String(b.body||'').trim();if(!doctorId||!subject||!body)return NextResponse.json({error:'Doktor, konu ve soru gerekli'},{status:400});const rel=await prisma.appointment.findFirst({where:{userId:u.id,doctorId}});if(!rel)return NextResponse.json({error:'Bu doktora soru göndermek için randevu ilişkisi gerekli'},{status:403});const row=await prisma.patientDoctorQuestion.create({data:{patientId:u.id,doctorId,subject:subject.slice(0,160),body:body.slice(0,2000)}});const d=await prisma.doctor.findUnique({where:{id:doctorId},select:{userId:true}});if(d?.userId)await prisma.notification.create({data:{userId:d.userId,title:'Yeni hasta sorusu',body:`${u.name}: ${subject.slice(0,80)}`,kind:'clinical_question'}});return NextResponse.json(row,{status:201})}
export async function PATCH(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const b=await req.json(),id=String(b.id||'');const row=await prisma.patientDoctorQuestion.findUnique({where:{id}});if(!row)return NextResponse.json({error:'Soru bulunamadı'},{status:404});if(u.role==='DOCTOR'&&u.doctorProfile?.id===row.doctorId){const answer=String(b.answer||'').trim();if(!answer)return NextResponse.json({error:'Yanıt gerekli'},{status:400});const updated=await prisma.patientDoctorQuestion.update({where:{id},data:{answer:answer.slice(0,2500),status:'answered',answeredAt:new Date()}});await prisma.notification.create({data:{userId:row.patientId,title:'Doktorun sorunu yanıtladı',body:row.subject,kind:'clinical_question_answer'}});return NextResponse.json(updated)}if(u.role==='PATIENT'&&u.id===row.patientId){return NextResponse.json(await prisma.patientDoctorQuestion.update({where:{id},data:{status:b.status==='closed'?'closed':row.status}}))}return NextResponse.json({error:'Erişim yok'},{status:403})}
