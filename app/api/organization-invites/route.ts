import {NextResponse} from 'next/server';import {currentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});if(u.role!=='DOCTOR')return NextResponse.json([]);return NextResponse.json(await prisma.organizationDoctorInvite.findMany({where:{status:'PENDING',OR:[{doctorUserId:u.id},{email:{equals:u.email,mode:'insensitive'}}]},include:{organization:{select:{id:true,name:true,type:true,city:true,status:true,isPublished:true}}},orderBy:{createdAt:'desc'}}))}
