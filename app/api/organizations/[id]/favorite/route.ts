import {NextResponse} from 'next/server';import {currentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({favorite:false});const {id}=await params;const org=await prisma.organization.findFirst({where:{OR:[{id},{slug:id}]}});if(!org)return NextResponse.json({favorite:false});return NextResponse.json({favorite:!!await prisma.organizationFavorite.findUnique({where:{userId_organizationId:{userId:u.id,organizationId:org.id}}})})}
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const {id}=await params;const org=await prisma.organization.findFirst({where:{OR:[{id},{slug:id}],status:'APPROVED',isPublished:true}});if(!org)return NextResponse.json({error:'Kurum bulunamadı'},{status:404});await prisma.organizationFavorite.upsert({where:{userId_organizationId:{userId:u.id,organizationId:org.id}},update:{},create:{userId:u.id,organizationId:org.id}});return NextResponse.json({favorite:true})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const {id}=await params;const org=await prisma.organization.findFirst({where:{OR:[{id},{slug:id}]}});if(org)await prisma.organizationFavorite.deleteMany({where:{userId:u.id,organizationId:org.id}});return NextResponse.json({favorite:false})}
