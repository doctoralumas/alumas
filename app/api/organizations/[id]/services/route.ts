import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";
async function canManage(id:string,u:any){return u?.role==='ADMIN'||!!(u&&await prisma.organization.findFirst({where:{id,ownerUserId:u.id}}))}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;return NextResponse.json(await prisma.organizationService.findMany({where:{organizationId:id,isActive:true},orderBy:{name:'asc'}}))}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const {id}=await params;if(!await canManage(id,u))return NextResponse.json({error:"Yetki yok"},{status:403});const b=await req.json();if(!b.name)return NextResponse.json({error:"Hizmet adı gerekli"},{status:400});return NextResponse.json(await prisma.organizationService.create({data:{organizationId:id,name:String(b.name),description:b.description||null,price:b.price==null||b.price===''?null:Number(b.price)}}),{status:201})}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const {id}=await params;if(!await canManage(id,u))return NextResponse.json({error:'Yetki yok'},{status:403});const b=await req.json();if(!b.serviceId)return NextResponse.json({error:'serviceId gerekli'},{status:400});const exists=await prisma.organizationService.findFirst({where:{id:String(b.serviceId),organizationId:id}});if(!exists)return NextResponse.json({error:'Hizmet bulunamadı'},{status:404});return NextResponse.json(await prisma.organizationService.update({where:{id:exists.id},data:{name:b.name?String(b.name):undefined,description:b.description===undefined?undefined:(b.description||null),price:b.price===undefined?undefined:(b.price===''?null:Number(b.price)),isActive:b.isActive===undefined?undefined:!!b.isActive}}))}
