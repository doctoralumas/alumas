import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {currentUser} from "@/lib/auth";export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const {id}=await params;const org=await prisma.organization.findFirst({where:u.role==="ADMIN"?{id}:{id,ownerUserId:u.id}});if(!org)return NextResponse.json({error:"Yetkisiz"},{status:403});const b=await req.json();return NextResponse.json(await prisma.organizationEmergencyService.create({data:{organizationId:id,name:String(b.name),kind:String(b.kind||"EMERGENCY"),phone:b.phone||null,is24Hours:!!b.is24Hours,description:b.description||null}}))}