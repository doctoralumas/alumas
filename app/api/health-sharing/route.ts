import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";const allowed=["measurements","labs","documents","imaging","care_plans"];export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});if(u.role==="PATIENT")return NextResponse.json(await prisma.healthShareConsent.findMany({where:{patientId:u.id},include:{doctor:true}}));if(u.role==="DOCTOR"&&u.doctorProfile)return NextResponse.json(await prisma.healthShareConsent.findMany({where:{doctorId:u.doctorProfile.id,status:"active"},include:{patient:true}}));return NextResponse.json([])}export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});const {doctorId,scopes=[]}=await req.json();const clean=scopes.filter((s:string)=>allowed.includes(s));const row=await prisma.healthShareConsent.upsert({where:{patientId_doctorId:{patientId:u.id,doctorId}},update:{scopes:clean,status:"active"},create:{patientId:u.id,doctorId,scopes:clean}});return NextResponse.json(row)}export async function DELETE(req:Request){const u=await currentUser();if(!u||u.role!=="PATIENT")return NextResponse.json({error:"Hasta hesabı gerekli"},{status:403});const {doctorId}=await req.json();await prisma.healthShareConsent.updateMany({where:{patientId:u.id,doctorId},data:{status:"revoked"}});return NextResponse.json({ok:true})}