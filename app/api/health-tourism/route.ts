import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";export async function GET(req:Request){const u=new URL(req.url),q=u.searchParams.get('q')||'';const where:any={isPublished:true};if(q)where.OR=[{title:{contains:q,mode:'insensitive'}},{category:{contains:q,mode:'insensitive'}},{city:{contains:q,mode:'insensitive'}},{providerName:{contains:q,mode:'insensitive'}},{agency:{name:{contains:q,mode:'insensitive'}}}];return NextResponse.json(await prisma.healthTourismPackage.findMany({where,include:{agency:true},orderBy:[{city:'asc'},{title:'asc'}],take:100}))}