import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {audit} from "@/lib/audit";
export async function GET(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const data=await prisma.user.findUnique({where:{id:u.id},select:{id:true,name:true,email:true,phone:true,role:true,birthDate:true,createdAt:true,appointments:{include:{doctor:true}},healthEntries:true,documents:{select:{id:true,title:true,category:true,fileName:true,mimeType:true,sizeBytes:true,createdAt:true}},labResults:true,sentMessages:true,receivedMessages:true,notifications:true,carePlans:{include:{items:true,doctor:true}},healthShares:{include:{doctor:true}},healthIntegrations:true,privacyConsents:true,bloodPressureReadings:true,glucoseReadings:true,sleepRecords:true,medications:{include:{reminders:true,doseLogs:true}},reminders:true,healthTargets:true,medicationDoseLogs:true,imagingResults:true,diagnosticRequests:true,emergencyHealthCard:true,vaccinations:true,allergies:true,medicalConditions:true,procedureHistory:true,specialHealthProfiles:true,childGrowthRecords:true,pregnancyEvents:true,healthSummaryShares:{include:{doctor:true}}}});await audit({actorUserId:u.id,action:"account.export",entityType:"User",entityId:u.id,req});return new NextResponse(JSON.stringify({exportedAt:new Date().toISOString(),data},null,2),{headers:{"Content-Type":"application/json; charset=utf-8","Content-Disposition":`attachment; filename="alumas-data-${u.id}.json"`,"Cache-Control":"private, no-store"}})}
