import { NextResponse } from "next/server"; import { currentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma";
export async function GET(){const u=await currentUser();if(!u||u.role!=="DOCTOR"||!u.doctorProfile)return NextResponse.json({error:"Yetkisiz"},{status:403}); const rows=await prisma.availability.findMany({where:{doctorId:u.doctorProfile.id,startsAt:{gte:new Date()}},orderBy:{startsAt:"asc"}});return NextResponse.json(rows)}
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=="DOCTOR"||!u.doctorProfile)return NextResponse.json({error:"Yetkisiz"},{status:403});const {startsAt,endsAt,type}=await req.json();if(!startsAt||!endsAt||new Date(endsAt)<=new Date(startsAt))return NextResponse.json({error:"Geçersiz zaman"},{status:400});try{const row=await prisma.availability.create({data:{doctorId:u.doctorProfile.id,startsAt:new Date(startsAt),endsAt:new Date(endsAt),type:type||"both"}});return NextResponse.json(row,{status:201})}catch{return NextResponse.json({error:"Bu başlangıç saati zaten mevcut."},{status:409})}}
