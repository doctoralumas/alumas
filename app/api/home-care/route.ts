import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});return NextResponse.json(await prisma.homeCareRequest.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"}}))}
export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"Giriş gerekli"},{status:401});const b=await req.json();if(!b.serviceType||!b.city)return NextResponse.json({error:"Hizmet ve şehir gerekli"},{status:400});return NextResponse.json(await prisma.homeCareRequest.create({data:{userId:u.id,serviceType:b.serviceType,city:b.city,district:b.district||null,addressNote:b.addressNote||null,preferredAt:b.preferredAt?new Date(b.preferredAt):null,note:b.note||null}}),{status:201})}
