"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import Link from "next/link";
export default function Page(){const params=useParams<{id:string}>(),id=String(params.id);const [data,setData]=useState<any>(null),[msg,setMsg]=useState('');const load=()=>fetch('/api/health/family-tasks?profileId='+id).then(r=>r.ok?r.json():null).then(setData);useEffect(()=>{load()},[id]);async function addTask(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/health/family-tasks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({profileId:id,title:f.get('title'),date:new Date(String(f.get('date'))).toISOString()})});const j=await r.json();setMsg(r.ok?'Hatırlatıcı eklendi.':j.error||'Eklenemedi');if(r.ok){e.currentTarget.reset();load()}}if(!data)return <div className="page"><div className="empty">Aile profili yükleniyor…</div></div>;const p=data.profile;const canRemind=data.access?.owner||data.access?.permissions?.includes('REMINDERS');return <div className="page"><div className="page-title"><span className="kicker">{p.type==='CHILD'?'Çocuk profili':'Gebelik profili'}</span><h1>{p.name}</h1><p>Bu profile bağlı randevu, aşı ve sağlık görevlerini ayrı takip et.</p></div><div className="family-profile-actions-v22"><Link className="primary" href={`/doctors?profileId=${id}`}>Doktor bul</Link><Link className="secondary" href={`/health/family?profile=${id}`}>Ölçüm / gebelik kaydı</Link><Link className="secondary" href={`/health/vaccinations?profileId=${id}`}>Aşılar</Link><Link className="secondary" href={`/health/family/${id}/calendar`}>Profil takvimi</Link></div><section className="panel health-section"><h2>Yeni sağlık görevi</h2>{canRemind?<form className="family-task-form-v22" onSubmit={addTask}><input name="title" placeholder="Örn. ölçüm, kontrol, aşı teyidi" required/><input name="date" type="datetime-local" required/><button className="primary">Hatırlat</button></form>:<p className="muted">Bu profilde hatırlatıcı oluşturma iznin yok.</p>}{msg&&<div className="notice-v14">{msg}</div>}</section><div className="dashboard-grid"><section className="panel"><h2>Randevular</h2>{data.appointments.map((a:any)=><div className="family-row-v22" key={a.id}><div><b>{a.doctor.name}</b><span>{a.doctor.specialty} · {new Date(a.startsAt).toLocaleString('tr-TR')}</span></div></div>)}{!data.appointments.length&&<div className="empty">Bu profile bağlı randevu yok.</div>}</section><section className="panel"><h2>Hatırlatıcılar</h2>{data.reminders.map((r:any)=><div className="family-row-v22" key={r.id}><div><b>{r.title}</b><span>{r.timeOfDay}</span></div></div>)}{!data.reminders.length&&<div className="empty">Hatırlatıcı yok.</div>}</section><section className="panel"><h2>Aşılar</h2>{data.vaccinations.map((v:any)=><div className="family-row-v22" key={v.id}><div><b>{v.vaccineName}</b><span>{new Date(v.administeredAt).toLocaleDateString('tr-TR')}{v.nextDoseAt?` · sonraki ${new Date(v.nextDoseAt).toLocaleDateString('tr-TR')}`:''}</span></div></div>)}{!data.vaccinations.length&&<div className="empty">Bu profile bağlı aşı kaydı yok.</div>}</section>{p.type==='PREGNANCY'&&<section className="panel"><h2>Gebelik planı</h2>{data.pregnancyEvents.map((x:any)=><div className="family-row-v22" key={x.id}><div><b>{x.title}</b><span>{new Date(x.startsAt).toLocaleString('tr-TR')}</span></div></div>)}{!data.pregnancyEvents.length&&<div className="empty">Plan kaydı yok.</div>}</section>}</div></div>}
