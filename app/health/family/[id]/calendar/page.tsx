"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import Link from "next/link";
export default function Page(){const {id}=useParams<{id:string}>();const [date,setDate]=useState(()=>new Date().toISOString().slice(0,10)),[d,setD]=useState<any>(null);useEffect(()=>{fetch(`/api/calendar?date=${date}&profileId=${encodeURIComponent(String(id))}`).then(r=>r.ok?r.json():null).then(setD)},[date,id]);return <div className="page"><div className="page-title"><span className="kicker">Aile profili</span><h1>Profil Sağlık Takvimi</h1><p>Yalnızca bu aile profiline bağlı randevu, aşı ve hatırlatıcıları görüntüle.</p></div><div className="date-filter-v16"><label>Tarih<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><Link className="secondary" href={`/health/family/${id}`}>Profile dön</Link></div>{!d?<div className="empty">Takvim yükleniyor…</div>:<div className="timeline-v22"><section className="panel"><h2>Randevular</h2>{d.appointments.map((x:any)=><div className="family-row-v22" key={x.id}><div><b>{x.doctor.name}</b><span>{new Date(x.startsAt).toLocaleString('tr-TR')}</span></div></div>)}{!d.appointments.length&&<div className="empty">Randevu yok.</div>}</section><section className="panel"><h2>Hatırlatıcılar</h2>{d.reminders.map((x:any)=><div className="family-row-v22" key={x.id}><div><b>{x.title}</b><span>{x.timeOfDay}</span></div></div>)}{!d.reminders.length&&<div className="empty">Hatırlatıcı yok.</div>}</section><section className="panel"><h2>Aşı / gebelik</h2>{d.vaccinations.map((x:any)=><div className="family-row-v22" key={x.id}><div><b>{x.vaccineName}</b><span>{x.nextDoseAt?new Date(x.nextDoseAt).toLocaleDateString('tr-TR'):'Aşı kaydı'}</span></div></div>)}{d.pregnancyEvents.map((x:any)=><div className="family-row-v22" key={x.id}><div><b>{x.title}</b><span>{new Date(x.startsAt).toLocaleString('tr-TR')}</span></div></div>)}{!d.vaccinations.length&&!d.pregnancyEvents.length&&<div className="empty">Kayıt yok.</div>}</section></div>}</div>}
