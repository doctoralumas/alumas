"use client";import {useEffect,useState} from "react";type R={id:string;weightKg?:number;heightCm?:number;waistCm?:number;measuredAt:string};export default function Page(){const [rows,setRows]=useState<R[]>([]);const load=()=>fetch('/api/health/body').then(r=>r.json()).then(x=>setRows(Array.isArray(x)?x:[]));useEffect(() => { load(); },[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),b=Object.fromEntries(f.entries());await fetch('/api/health/body',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});e.currentTarget.reset();load()}return <div className="page"><div className="page-title"><span className="kicker">Yetişkin takip</span><h1>Kilo & Boy</h1><p>Boy, kilo ve bel çevresi kayıtlarını tarih bazında sakla.</p></div><form className="panel v24-form" onSubmit={submit}><input name="weightKg" type="number" step="0.1" placeholder="Kilo (kg)"/><input name="heightCm" type="number" step="0.1" placeholder="Boy (cm)"/><input name="waistCm" type="number" step="0.1" placeholder="Bel (cm)"/><button className="primary">Kaydet</button></form><section className="panel"><h2>Geçmiş</h2>{rows.map(r=><div className="share-row" key={r.id}><div><b>{r.weightKg?`${r.weightKg} kg`:''} {r.heightCm?`· ${r.heightCm} cm`:''}</b><small>{r.waistCm?`Bel ${r.waistCm} cm · `:''}{new Date(r.measuredAt).toLocaleDateString('tr-TR')}</small></div></div>)}{!rows.length&&<div className="empty">Henüz ölçüm yok.</div>}</section></div>}
