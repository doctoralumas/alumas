"use client";import {useEffect,useState} from "react";
type A={id:string;allergen:string;category?:string;reaction?:string;severity:string;isActive:boolean};
export default function Page(){const [rows,setRows]=useState<A[]>([]),[open,setOpen]=useState(false);const load=()=>fetch('/api/health/allergies').then(r=>r.ok?r.json():[]).then(setRows);useEffect(() => { load(); },[]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/health/allergies',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});if(r.ok){setOpen(false);e.currentTarget.reset();load()}}async function toggle(a:A){await fetch('/api/health/allergies',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:a.id,isActive:!a.isActive})});load()}return <div className="page"><div className="page-title row between"><div><span className="kicker">Tıbbi geçmiş</span><h1>Alerjilerim</h1><p>Bilinen alerjilerini, reaksiyonlarını ve aktiflik durumunu kaydet.</p></div><button className="primary" onClick={()=>setOpen(!open)}>+ Alerji ekle</button></div>{open&&<form className="inline-form" onSubmit={add}><input name="allergen" placeholder="Alerjen" required/><select name="category"><option>İlaç</option><option>Gıda</option><option>Çevresel</option><option>Diğer</option></select><input name="reaction" placeholder="Reaksiyon"/><select name="severity"><option value="unknown">Belirtilmedi</option><option value="mild">Hafif</option><option value="moderate">Orta</option><option value="severe">Şiddetli</option></select><button className="primary">Kaydet</button></form>}<section className="panel health-section"><div className="timeline-list">{rows.map(a=><div className="timeline-row" key={a.id}><div><b>{a.allergen}</b><span>{a.category||'Alerji'} · {a.reaction||'Reaksiyon belirtilmedi'}</span></div><div className="row"><span className={`status-pill ${a.isActive?'active':'cancelled'}`}>{a.isActive?'Aktif':'Geçmiş'}</span><button className="secondary small" onClick={()=>toggle(a)}>{a.isActive?'Geçmişe al':'Aktif yap'}</button></div></div>)}{!rows.length&&<div className="empty">Henüz alerji kaydı yok.</div>}</div></section></div>}
