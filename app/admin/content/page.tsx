import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import ContentAdminClient from "@/components/content-admin-client";export default async function Page(){const u=await currentUser();if(!u)redirect("/login");if(u.role!=="ADMIN")redirect("/profile");return <div className="page"><div className="page-title"><span className="kicker">CMS</span><h1>İçerik & Banner Yönetimi</h1><p>Ana sayfa slider’ı, kategori şablonları ve iletişim talepleri.</p></div><ContentAdminClient/></div>}