import AuditLogViewer from "@/components/audit-log-viewer";import { redirect } from "next/navigation"; import { currentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma";
export default async function Admin(){const u=await currentUser();if(!u)redirect('/login');if(u.role!=="ADMIN")redirect('/profile');const [users,doctors,appointments]=await Promise.all([prisma.user.count(),prisma.doctor.count(),prisma.appointment.count()]);const recent=await prisma.user.findMany({orderBy:{createdAt:'desc'},take:10});return <div className="page"><div className="page-title row between"><div><span className="kicker">Yönetim</span><h1>Alumas Admin</h1><p>Platformun temel operasyon görünümü.</p></div><div className="row"><a className="secondary" href="/admin/doctors">Doktor onayları</a><a className="secondary" href="/admin/agencies">Acente onayları</a><a className="secondary" href="/admin/content">İçerik & banner</a><a className="secondary" href="/admin/reviews">Yorum moderasyonu</a><a className="primary" href="/admin/organizations">Kurum onayları</a><a className="secondary" href="/admin/release">Release dashboard</a></div></div><div className="admin-stats"><div className="metric"><span>Kullanıcı</span><strong>{users}</strong><small>toplam hesap</small></div><div className="metric"><span>Uzman</span><strong>{doctors}</strong><small>aktif profil</small></div><div className="metric"><span>Randevu</span><strong>{appointments}</strong><small>toplam kayıt</small></div></div><section className="panel"><h2>Son kullanıcılar</h2><div className="slot-list">{recent.map(x=><div className="slot-row" key={x.id}><div><b>{x.name}</b><span>{x.email}</span></div><span className="status">{x.role}</span></div>)}</div></section><section className="panel health-section"><h2>Audit kayıtları</h2><p>Son güvenlik ve gizlilik işlemleri.</p><AuditLogViewer/></section></div>}
