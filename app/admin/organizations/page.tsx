import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import AdminOrganizations from "@/components/admin-organizations";export default async function AdminOrgs(){const u=await currentUser();if(!u)redirect('/login');if(u.role!=='ADMIN')redirect('/profile');return <div className="page"><div className="page-title"><span className="kicker">Yönetim</span><h1>Kurum başvuruları</h1><p>Başvuruları doğrula, onayla veya reddet.</p></div><section className="panel"><AdminOrganizations/></section></div>}
