import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import AdminDoctorList from "@/components/admin-doctor-list";export default async function Page(){const u=await currentUser();if(!u)redirect("/login");if(u.role!=="ADMIN")redirect("/profile");return <div className="page"><div className="page-title"><span className="kicker">Doğrulama</span><h1>Doktor başvuruları</h1></div><section className="panel"><AdminDoctorList/></section></div>}