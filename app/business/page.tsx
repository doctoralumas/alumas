import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import BusinessDashboard from "@/components/business-dashboard";export default async function Business(){if(!await currentUser())redirect('/login');return <div className="page"><div className="page-title row between"><div><span className="kicker">Alumas Business</span><h1>Kurum Paneli</h1><p>Başvuru durumunu ve kurum verilerini yönet.</p></div><a className="primary" href="/business/apply">+ Yeni kurum</a></div><BusinessDashboard/></div>}
