import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import BusinessApplicationForm from "@/components/business-application-form";export default async function Apply(){if(!await currentUser())redirect("/login");return <div className="page"><div className="page-title"><span className="kicker">Alumas Business</span><h1>Kurumsal hesap oluştur</h1><p>Hastane, klinik veya eczane başvurunuz doğrulamadan sonra yayınlanır.</p></div><section className="panel"><BusinessApplicationForm/></section></div>}
