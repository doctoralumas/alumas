import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import BusinessApplicationForm from "@/components/business-application-form";export default async function Apply({searchParams}:{searchParams:Promise<{type?:string}>}){if(!await currentUser())redirect("/login");const {type}=await searchParams;return <div className="page"><div className="page-title"><span className="kicker">Alumas Business</span><h1>Kurumsal hesap oluştur</h1><p>Hastane, klinik, eczane, görüntüleme merkezi veya tıbbi laboratuvar başvurunuz doğrulamadan sonra yayınlanır.</p></div><section className="panel"><BusinessApplicationForm initialType={type}/></section></div>}
