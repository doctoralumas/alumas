"use client";import {useEffect,useRef,useState} from "react";export default function Page({params}:{params:Promise<{id:string}>}){const [id,setId]=useState(''),[room,setRoom]=useState<any>(null),[err,setErr]=useState('');const video=useRef<HTMLVideoElement>(null);useEffect(()=>{params.then(p=>{setId(p.id);fetch('/api/video-consultations/'+p.id).then(r=>r.json()).then(setRoom)})},[params]);async function camera(){try{const s=await navigator.mediaDevices.getUserMedia({video:true,audio:true});if(video.current)video.current.srcObject=s}catch{setErr('Kamera/mikrofon izni verilemedi.')}}return <div className="page"><div className="page-title"><span className="kicker">Güvenli görüşme</span><h1>Görüntülü Görüşme</h1><p>{room?.doctor?.name||'Uzman görüşmesi'} · sağlayıcı: {room?.provider||'yükleniyor'}</p></div>{room?.roomUrl?<section className="panel"><iframe className="v24-video-frame" src={room.roomUrl} allow="camera; microphone; fullscreen; speaker; display-capture"/><p>Oda, yapılandırılmış video sağlayıcısı üzerinden açıldı.</p></section>:<section className="panel"><h2>Geliştirme odası</h2><p>Gerçek görüşme için VIDEO_PROVIDER ve sağlayıcı API anahtarı yapılandırılmalı. Burada cihaz kamera/mikrofon testi yapabilirsin.</p><video className="v24-local-video" ref={video} autoPlay muted playsInline/><button className="primary" onClick={camera}>Kamera & mikrofonu test et</button>{err&&<p>{err}</p>}</section>}</div>}
