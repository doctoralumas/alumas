import fs from "node:fs";let ok=true;for(const f of ["INCIDENT_RUNBOOK.md","BACKUP_POLICY.md","scripts/backup-db.mjs","scripts/verify-backup.mjs","scripts/restore-db.mjs"]){if(!fs.existsSync(f)){console.error("missing",f);ok=false}}const restore=fs.readFileSync("scripts/restore-db.mjs","utf8");if(!restore.includes("--confirm=RESTORE")){console.error("restore confirmation guard missing");ok=false}if(!ok)process.exit(1);console.log("ops check OK");